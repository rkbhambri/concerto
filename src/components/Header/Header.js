import React from 'react';
import './Header.css';

const Header = (props) => {
    return (
        <div className="header">
            <p>Concerto AI</p>
        </div>
    );
};

export default Header;
